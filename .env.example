# Server-side only — Upstash Redis REST API credentials.
# Get these from the Upstash console (your database → REST API section).
# Set them in Vercel project settings as well for the deployed API route.
UPSTASH_REDIS_REST_URL=
UPSTASH_REDIS_REST_TOKEN=
