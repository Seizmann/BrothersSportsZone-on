# dependencies
node_modules/

# env — server-side secrets only (Upstash Redis credentials)
.env
.env.local
.env.*.local

# build output
dist/

# guarded project docs — never commit or push
REQUIREMENT.md
DESIGN.md
AGENT.md
CLAUDE.md

# logs / OS
*.log
.DS_Store

# tooling artifacts
.zcode/
tsconfig.tsbuildinfo
